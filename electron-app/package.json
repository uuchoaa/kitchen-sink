{
  "name": "electron-scraper",
  "version": "2.0.0",
  "description": "Modular Electron app for web scraping with extensible sources and actions",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && cp src/ui/w2.html dist/ui/w2.html",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron . --enable-logging",
    "watch": "tsc --watch"
  },
  "keywords": ["electron", "scraper", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^28.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {}
}

